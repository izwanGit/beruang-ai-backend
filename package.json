{
  "name": "beruang-ai-backend",
  "version": "2.0.0",
  "description": "Unified AI backend handling Transaction Classification & Chat Intent Analysis",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "train:transaction": "node train_transaction.js",
    "train:intent": "node train_intent.js",
    "gen:intent": "node generate_intent_dataset.js",
    "setup": "npm install && npm run gen:intent && npm run train:intent && npm run train:transaction"
  },
  "dependencies": {
    "@tensorflow/tfjs-node": "^4.22.0",
    "csv-parser": "^3.2.0",
    "express": "^5.1.0",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
